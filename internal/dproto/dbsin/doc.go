// Package dbsin contains the types for handling protocol bootstrap,
// speifically accepting an inbound connection
// before we have received the first message on the bootstrap stream.
package dbsin
